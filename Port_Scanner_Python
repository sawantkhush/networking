#Basic_Port_Scanning_Using_Python
import socket
target = "127.0.0.1"
print("Scannin all TCP ports)
for port in range(1,40):
  s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
  s.settimeout(0.5)

  if s.connect_ex((target,port)) == 0:
    print(f"Port {port} OPEN")
  s.close()
  
